# AI CCTV 배치 적정성 평가 모델 — MVP 구현 스펙

> 이 문서는 Claude Code에게 전달하는 구현 지침이다. 읽고 바로 작업을 시작할 것.
> 대회 제출 마감 D+14. 연구가 아니라 **제출 가능한 산출물**을 만드는 것이 목표다.

---

## 0. 에이전트 준수사항 (먼저 읽을 것)

### 0.1 절대 규칙

1. **숫자를 지어내지 말 것.** 실행하지 않은 계산의 결과를 문서·주석·JSON에 쓰지 않는다. 값이 없으면 `null`을 넣고 `"status": "not_computed"`를 함께 기록한다. 이 프로젝트는 대회 제출물이며, 검증되지 않은 수치가 하나라도 섞이면 전체 신뢰도가 무너진다.
2. **스코프를 넓히지 말 것.** §6의 제외 목록에 있는 것은 "나중에 하면 좋겠다"는 주석조차 남기지 않는다. 남은 기간이 13일이다.
3. **환경 문제로 시간을 쓰지 말 것.** 로컬 CUDA 세팅 금지. 추론은 Colab 또는 CPU. 30분 이상 환경 문제로 막히면 진행을 멈추고 사용자에게 보고한다.
4. **Windows 환경이다.** 경로는 `pathlib.Path`만 사용한다. 셸 파이프·`&&` 체인·POSIX 전용 명령을 스크립트에 넣지 않는다.
5. **외부 API 키가 필요한 작업은 없다.** 키를 요구하는 라이브러리를 도입하지 않는다.

### 0.2 작업 방식

- 각 Phase는 **통과 조건(Acceptance)** 을 충족해야 다음으로 넘어간다.
- Phase 완료 시 `PROGRESS.md`에 결과 요약(실제 수치 포함)을 append 한다.
- 커밋은 Phase 단위. 메시지는 `phase{N}: <한 줄 요약>`.

---

## 1. 무엇을 만드는가

건설현장 CCTV 배치도와 골조 형상을 입력받아, 각 위험구역에서 AI가 **안전모 미착용을 실제로 검출할 확률**을 계산하고, 그 값으로 배치의 적정 여부를 판정하는 도구.

기존 도구는 "보이는가"(기하학적 가시성)를 계산한다. 이 도구는 "검출되는가"를 계산한다. 그 차이가 전부다.

### 왜 필요한가 (배경 — 코드에는 안 들어가지만 판단 기준이 됨)

- 국토교통부·국토안전관리원 「스마트 안전장비 활용 가이드라인」('26.5 개정, 88쪽)의 AI CCTV 기준은 **「2-2 도입기준」 16쪽 표 4번** 한 줄이다 — *"「지능형 CCTV 성능 시험인증」 쓰러짐 분야의 인증을 취득한 제품"*, 비고란은 **권장기준**. 17쪽 상세표도 같다. 필수기준은 무선통신·배터리·보호구 안전인증 셋뿐이고 **AI CCTV 는 필수가 아니다.**
  - 원문: `docs/reference/스마트안전장비_활용가이드라인.pdf` (스캔본이라 텍스트 추출 불가. 쪽 이미지로 확인했다)
  - **발행처가 LH 가 아니다.** 종전에 "LH 가이드라인"으로 적었던 것은 틀렸다.
- 그 90%는 가이드라인이 아니라 **KISA 「지능형 CCTV 성능 시험인증」** 의 기준이다. KISA 원문: *"시험 기준 : 각 항목별 **90% 이상 검출 정확도** 확보"*. **KISA 가 구축한 인증용 영상 DB 위에서 KISA 시험도구가 산출하는 점수**이며, 현장 설치 조건과 무관하다.
- **가이드라인 전체에 커버리지·화각·사각지대·해상도 수치 기준이 없다.** 「3-3 점검 및 평가방안」(64쪽)의 체크리스트도 *"스마트 안전관리 시스템이 정상작동 하는가?"* 같은 O/X 문항이며 수치가 없다.
- LH 자체 사용자 평가(2025.12)에서 AI CCTV는 토목·조성 공사에서 "개선 집중기술"로 분류되었고, 처방은 "성능 개선 또는 운용 방식 조정이 필요"였다. 무엇을 조정하라는지는 없다.

**이 도구가 그 "적정"을 수치로 정의한다.**

---

## 2. 산출물 3개

| # | 산출물 | 파일 | 성격 |
|---|---|---|---|
| 1 | P_detect 곡선 | `outputs/curve_params.json` | 실험 결과 |
| 2 | 위험가중 검출률(WDR) 평가 | `outputs/site_eval.json` | 제도 접목 지표 |
| 3 | 심사자 화면 목업 | `mockup/index.html` | PT 승부처 |

최종 목표 문장 (§7 Phase 3에서 실제 값이 채워져야 함):

> 기하 커버리지 기준 최적 배치의 위험가중 안전모 검출률은 **61.0%** 였다. 동일 대수를 검출확률 기준으로 재배치하면 **70.6%** 가 된다.

**채워짐 (2026-08-14, Phase 3).** 미달구역도 414개 → 162개로 줄었다.
출처는 `outputs/comparison.json` 이며 8대 동일 예산, 두 배치를 모두 검출확률의 자로 재측정한 값이다.

---

## 3. 리포지토리 구조

```
aicctv-eval/
├─ CLAUDE.md                  # 이 문서
├─ PROGRESS.md                # Phase별 실행 결과 로그 (에이전트가 갱신)
├─ requirements.txt
├─ config.py                  # 전역 상수 + 폴백 스위치
├─ data/
│  ├─ raw/                    # SHWD 원본 (git ignore)
│  └─ filtered/               # 머리 bbox >= 40px 필터 통과분
├─ notebooks/
│  └─ 01_experiment.ipynb     # Colab 실행용
├─ src/
│  ├─ transforms.py           # ρ/θ/o 변형
│  ├─ run_grid.py             # 125조건 추론
│  ├─ fit_curve.py            # 분리형 곱셈 모델 피팅
│  ├─ site.py                 # 가상 현장 정의
│  ├─ geometry.py             # 광선투사 → (ρ, θ, o)
│  ├─ detect_model.py         # 곡선 적용
│  ├─ aggregate.py            # P_total, WDR
│  ├─ optimize.py             # 탐욕 최적화 2종
│  └─ report.py               # JSON 출력
├─ outputs/
│  ├─ grid_results.csv
│  ├─ curve_params.json
│  ├─ site_eval.json
│  └─ comparison.json
└─ mockup/
   ├─ index.html
   └─ data.json               # site_eval.json 사본
```

---

## 4. 실험 스펙 (Phase 1~2)

### 4.1 데이터

- 데이터셋: **SHWD** (Safety Helmet Wearing Dataset). 라벨 `hat` / `person`(미착용).
- 필터: 원본에서 **머리 bbox 짧은 변 ≥ 40px** 인 인스턴스만 사용. 기준 조건(고해상도)을 확보하기 위함.
- 목표 수량: **500장**. 부족하면 30px로 완화하되 `PROGRESS.md`에 기록.

### 4.2 3축 변형 격자

**변수는 3개다. 방위각 φ는 제외한다.** 안전모는 상방 시점에서 대략 원형이라 정면/후면 차이가 작고, 방위각이 결정적인 항목(안전대 고리 체결 등)은 이번 탐지 대상이 아니다. 4축이면 1,440조건, 3축이면 **288조건**이다.

> **ρ 수준 8·6·4 추가 (2026-08-14).** 원안은 12px 까지였다. 그런데 §5.2 의 4K·HFOV 90° 에서 `ρ = 480/d` 이므로 ρ=12px 가 **d=40m** 이고, 현장이 100×60m 라 복셀-카메라 쌍 대부분이 40m 를 넘는다. 그 구간을 측정 없이 로지스틱 외삽으로 채우면 WDR 이 통째로 부풀려진다(외삽 시 ρ=5px 에서 0.64 가 나왔는데 근거가 없다). 실측으로 덮는다. 조건 125 → 200.

> **θ·o 수준 75 추가 (2026-08-15).** 같은 이유다. ρ 에만 외삽 금지를 걸어두고 θ·o 는 범위 밖을 그대로 답하고 있었다. `geometry` 는 타워크레인(z=25m) 근거리에서 θ>60° 를, 비계 뒤에서 o>0.62 를 만든다. 한 축에만 원칙을 적용한 비대칭은 나머지 방어논리까지 의심받게 한다. 조건 200 → **288**. 실측 결과 θ=75 에서 `recall_nohat` 0.0146, o=75 에서 0.0 — 두 축 모두 그 구간에서 사실상 절벽이다.

| 축 | 수준 | 구현 |
|---|---|---|
| ρ 유효 픽셀밀도 | 머리 bbox 48 / 32 / 24 / 16 / 12 / **8 / 6 / 4** px | 목표 배율로 **다운샘플 → 원본 크기로 업샘플** (INTER_AREA → INTER_LINEAR) |
| θ 부감각 | 0 / 15 / 30 / 45 / 60 / **75**° | 호모그래피 워핑 |
| o 가림률 | 0 / 15 / 30 / 45 / 60 / **75** % | **수직 스트라이프 마스킹** |

**가림 마스킹은 랜덤 사각형이 아니라 수직 스트라이프여야 한다.** 실제 현장의 가림원은 비계·동바리·거푸집 지주로 대부분 수직 부재다. 스트라이프 폭과 간격을 조절해 목표 가림률을 맞춘다. 마스크 색은 회색(무채색) 고정.

> **스트라이프 주기는 결과를 지배하는 자유 파라미터다 (2026-08-15).** 같은 가림률이라도 부재가 촘촘할수록 급락한다 — o=30% 에서 주기 가로/12·/24·/48 이 각각 `recall_nohat` 0.443 / 0.239 / 0.045 다. 임의값은 아니며 `divisor ≈ W_scene / 1.8m = d/0.9` (지주 간격 1.8m) 로 유도되지만 거리에 의존한다. **거리 의존을 모델에 넣지 않는다** — 분리형 곱셈 모델이 깨진다. 대신 `src/sensitivity.py` 로 12/24/48 을 스윕해 ΔWDR 의 부호가 유지되는 것을 보인다.

호모그래피는 3D 시점 변화의 근사다. 이 한계는 제안서에 선제 기술하므로 코드에서 보정하려 하지 말 것.

### 4.3 검출 모델

- 주 모델: **YOLOv8n 을 SHWD train 분할(5,457장)로 파인튜닝한 가중치.**
  COCO 사전학습본에는 `hat`·`head` 클래스가 없어, 변형을 걸지 않은 기준 조건에서도
  주 지표가 **0.088** 에 그친다(실측). 곡선이 나오지 않으므로 파인튜닝이 전제다.
  학습 조건 50epoch · imgsz 640 · batch 16 · seed 0, **val mAP50 0.935**.
  가중치는 `runs/detect/shwd_yolov8n/weights/best.pt`.
- **실험셋은 test 분할에서만 뽑는다.** 학습에 쓴 이미지로 곡선을 재면 ρ·θ·o 의
  효과가 아니라 암기를 재게 된다.
- 대조 1회: 소형 VLM 1종 — 입력 리사이즈로 인한 소형 객체 병목을 보이기 위함. **동일 격자를 다 돌리지 말고 ρ축 5수준 × θ=0 × o=0, 총 5조건만** 실행한다.

### 4.4 측정 지표

조건별로 다음을 기록:

- `recall_hat` — 착용 인스턴스 재현율
- `recall_nohat` — 미착용 인스턴스 재현율 (**주 지표**)
- `conf_mean`, `conf_std` — 검출된 것들의 confidence 분포
- `n_gt`, `n_tp`, `n_fp`

IoU 임계 0.5, confidence 임계 0.25 고정.

### 4.5 곡선 피팅

**분리형 곱셈 모델**을 사용한다.

```
P(ρ, θ, o) = f(ρ) · g(θ) · h(o)
```

- `f(ρ)` = 로지스틱: `L / (1 + exp(-k(ρ - x0)))`
- `g(θ)` = 1에서 시작하는 단조 감소. 2차·지수·**로지스틱** 중 R²가 높은 쪽.
  2차에는 **계수에 `a ≤ 0, b ≤ 0` 제약을 건다** — 제약 없이 맞추면 θ=15 의 실측 융기
  (0.9592 > 0.9578, +0.0014)를 2차가 5% 로 증폭해 15.4° 에 꼭짓점이 생기고,
  최적화가 15° 내리깐 카메라를 정면보다 선호하게 된다.
  **θ=75 를 측정한 뒤로는 로지스틱이 이긴다 (2026-08-15)** — 45°까지 평평하다가
  60~75°에서 무너지는 절벽을 2차·지수는 맞히지 못한다.
  단면 R² 는 quadratic 0.8586 · exponential 0.5125 · **logistic 0.9973**.
  `g(θ) = (1+e^{-k·x0}) / (1 + e^{k(θ-x0)})`, k=0.2420 · **x0=61.36°**.
  형태가 단조 감소를 보장하므로 꼭짓점 문제도 함께 사라진다
- `h(o)` = 지수 감쇠: `exp(-λ·o)`.
  입력은 화면 전체 가림률이 아니라 **인스턴스 평균 가림률**(`occ_pct_box`)이다.
  §5.2 의 현장 `o` 가 개체 단위 값이라 정의를 맞춘 것이다. 두 정의의 실측 차이는
  0.4%p 이내였다(검증 완료, `src/occ_box.py`)

상호작용항은 넣지 않는다. 해석이 쉽고 최적화에 바로 들어가며, **1차 근사임을 제안서에 명시**하는 것이 방어 전략이다.

각 축은 다른 두 축을 기준값(ρ=48, θ=0, o=0)에 고정한 단면으로 먼저 피팅하고, 격자 전체 288점에 대해 R²를 계산해 검증한다.

### 4.6 출력 계약 — `curve_params.json`

```json
{
  "model": "separable_multiplicative",
  "target": "recall_nohat",
  "detector": "yolov8n",
  "detector_weights": "yolov8n.pt",
  "f_rho":   {"form": "logistic",  "L": null, "k": null, "x0": null},
  "g_theta": {"form": null,        "params": null},
  "h_occ":   {"form": "exp_decay", "lambda": null},
  "r2_full_grid": null,
  "n_conditions": 125,
  "n_images": null,
  "iou_thr": 0.5,
  "conf_thr": 0.25,
  "status": "not_computed",
  "generated_at": null
}
```

`null`은 실행 후 실제 값으로 교체한다. **교체 전에 다른 모듈이 이 파일을 소비하면 즉시 에러를 내야 한다** (`status != "ok"` 이면 `raise`).

---

## 5. 평가 파이프라인 (Phase 3)

```
[골조 형상]  [카메라 배치]  [위험도 가중치]
     └──────┬──────┘              │
            ▼                     │
   ① 복셀화 + 광선투사             │
      복셀별 (ρ, θ, o)             │
            ▼                     │
   ② P_detect 곡선 적용            │
      P(v, c)                     │
            ▼                     │
   ③ 다중 카메라 결합              │
      P_total = 1 − Π(1−P)        │
            ▼                     ▼
   ④ 위험가중 집계  ◄──────────────┘
      WDR = Σ w·P_total / Σ w
            ▼
   ⑤ 판정 + 미달구역 추출
            ▼
   ⑥ 재배치 대안 (탐욕)
```

### 5.1 가상 현장 (`site.py`)

- 규모: **100 m × 60 m**, LH 아파트 건설현장 1개 공구 모사
- 골조: 진행 중인 상태. 코어 벽체 + 슬래브 + 외곽 비계로 단순화. **BIM 파일 불필요, 코드로 직육면체 조합 생성**
- 복셀: **2 m 격자**, 작업면 높이대만 (z = 0.5~2.0 m 층)
- 위험 가중치 `w(v)`: LH 7대 유형 20개 위험공종에서 매핑
  - 갱폼 작업면, 개구부 주변, 리프트 승강구, 타워크레인 인양반경, 자재 야적장
  - 값은 상대 가중치 1~5의 정수. **자체 위험도 산정 알고리즘을 만들지 말 것** — LH·국토안전관리원 위험도 지수를 입력으로 받는 구조로 설계하고, MVP에서는 하드코딩된 값을 쓴다
- 카메라 후보 위치: **24개** (현장 경계 폴 + 코어 상부 + 타워크레인)
- 예산: **8대**

### 5.2 기하 계산 (`geometry.py`)

카메라 스펙: 4K (3840×2160), HFOV 90° 기본.

```
f_px = (W_img / 2) / tan(HFOV / 2)
ρ    = f_px * H_head / d          # H_head = 0.25 m, d = 카메라-복셀 거리
θ    = degrees(asin((z_cam - z_voxel) / d))
```

가림률 `o`:
- 복셀 위치에 **높이 1.7 m 수직 막대**(사람)를 세운다
- 막대를 따라 **11개 샘플점**을 잡고 각 점에서 카메라로 광선을 쏜다
- 골조 솔리드에 막히는 비율 = `o`
- 전부 막히면 `visible = false`, `P = 0`

출력 (복셀×카메라 쌍):

```json
{"voxel_id": "v_0123", "camera_id": "c_04",
 "d_m": 38.2, "rho_px": 27.4, "theta_deg": 38.0,
 "occ_ratio": 0.31, "visible": true}
```

### 5.3 집계 (`aggregate.py`)

```
P_total(v) = 1 − Π_c (1 − P(v, c))
WDR        = Σ_v w(v) · P_total(v) / Σ_v w(v)
```

`P_total`이 **복수 카메라 중첩으로 상승**하는 것이 이 모델의 동작 방식이다. 각 0.6인 두 대가 합쳐 0.84가 되므로, 고위험 구역에서는 중첩이 이득이다. **중첩을 페널티로 다루는 코드를 절대 넣지 말 것.**

> **ADDENDUM-01 §5.1 정정.** 종전에는 *"기존 연구(MDPI 2024)는 중첩 최소화를 목표로 하는데 우리는 반대"* 라고 적었다. **사실이 아니다** — 중첩이 검출률을 올린다는 것은 WSN target coverage 문헌의 표준 결과이며, RESPIRE(2020)도 coverage-only 접근이 단일 센서 의존을 낳는다고 보고했다. 이 반전을 **우리 발견으로 서술하지 말 것.** 코드 동작은 변경 없음.

판정 임계: `P_total < 0.5` 인 복셀을 **미달구역**으로 표시. 임계값은 `config.py`에서 조정 가능해야 하며, 0.5는 잠정값임을 주석에 명시.

### 5.4 최적화 (`optimize.py`)

**두 번 돌린다. 이게 논증 구조의 전부다.**

| 기준 | 목적함수 | 의미 |
|---|---|---|
| A. 기하 커버리지 | `Σ w(v) · 1[가시 ∧ ρ ≥ ρ_DORI]` | 기존 방식 |
| B. 검출확률 | `Σ w(v) · P_total(v)` | 제안 방식 |

둘 다 탐욕(greedy)으로 8대를 순차 선택. 그리고 **두 배치를 모두 B의 자(P_detect)로 재측정**하여 WDR을 비교한다.

> 기존 기준으로 잘 설계한 배치도 실제 검출률은 낮다 — 이것이 증명 대상이다.

> **A 의 가시 판정에 IEC 62676-4(DORI) 최소 픽셀밀도를 건다 (2026-08-15).** 원안대로 "화각 안 + 완전차폐 아님"만 보면 116m 떨어진 복셀도 커버로 잡힌다. 실무 설계도구(JVSG · Axis Site Designer)는 DORI 최소 PPM 을 지키므로, **기준선을 그 수준으로 올려야 비교가 성립한다.** 안 그러면 기존 방식을 실무보다 못하게 모델링해놓고 이긴 것이 되어 심사에서 무너진다.
>
> 환산은 `ρ[px] = PPM × H_head(0.25m)`, 즉 `PPM = 4ρ`. 네 등급을 전수 실행해 `site_eval.json` 의 `geometric_baseline.sweep` 에 싣고, **ΔWDR 이 가장 작은 등급(observation, 62.5 PPM = ρ 15.62px)을 기본값**으로 쓴다 — 기존 방식에 가장 유리한 기준선을 골라야 "약한 상대와 싸웠다"는 비판을 받지 않는다.
>
> recognition 이 observation 보다 나쁜 것은 오류가 아니다. 임계를 올리면 만족 가능한 복셀이 급감해(통과 쌍 28.4% → 6.1%) 탐욕이 오히려 나빠진다. **임계 방식 자체의 구조적 결함**이며 제안서 소재다.
>
> DORI 는 **인간 관찰자** 기준이며 AI 검출기에 대해 검증된 바 없다. 여기서는 기존 방식의 기준선을 세우는 용도로만 쓰고, 이 한계를 제안서에 명시한다.

목적함수 B는 submodular이므로 탐욕해가 최적해의 (1−1/e) ≈ 63% 이상을 보장한다. 이 성질을 주석에 명시할 것 (제안서에 인용됨).

### 5.5 출력 계약 — `site_eval.json`

```json
{
  "site": {"width_m": 100, "depth_m": 60, "voxel_m": 2},
  "camera_budget": 8,
  "threshold": 0.5,
  "placements": {
    "geometric": {"camera_ids": [], "WDR": null, "fail_voxel_count": null},
    "probabilistic": {"camera_ids": [], "WDR": null, "fail_voxel_count": null}
  },
  "voxels": [
    {"id": "v_0123", "x": 0, "y": 0, "w": 4,
     "P_total_geometric": null, "P_total_probabilistic": null, "pass": null}
  ],
  "fail_zones": [],
  "status": "not_computed"
}
```

---

## 6. 제외 목록 — 손대지 말 것

스코프 방어선이다. 심사에서 물으면 "제외했다"고 답해야지 "못 했다"고 하면 안 된다.

| 제외 항목 | 사유 |
|---|---|
| **4D (공정 단계별 카메라 재배치)** | 2025.10 ScienceDirect 논문과 정면 중복. **전략적 제외** |
| 방위각 φ | §4.2. 조건 5배 증가 대비 효용 낮음 |
| 야간·조명·기상 조건 | 변수 폭증 |
| PTZ 동적 제어 | 고정형 한정 |
| A-Eye 실제 모델 재현 | 접근 불가. **곡선만 교체하면 되는 구조**로 설계할 것 |
| 실시간 스트리밍·영상 파이프라인 | 배치 평가 도구지 관제 시스템이 아님 |
| 데이터베이스, 사용자 인증, 배포 | MVP 범위 밖 |

---

## 7. 실행 워크플로우 — 13일

### Phase 0 — 즉시 (D+0)

1. `aicctv-eval` 스캐폴딩 생성, `requirements.txt` 작성
2. SHWD 다운로드 및 `data/raw/` 배치
3. `config.py`에 전역 상수 + 폴백 스위치 `ENABLE_OPTIMIZATION = True`

**Acceptance:** `python -c "import ultralytics, cv2, numpy, scipy"` 통과.

### Phase 1 — 파이프라인 (D+1 ~ D+3)

1. `transforms.py` — 3축 변형 함수. 각 함수는 단일 이미지 + 파라미터 → 변형 이미지
2. 데이터 필터링 → `data/filtered/`, 목표 500장
3. **스모크 테스트: 125조건 중 9조건만** (ρ ∈ {48,24,12} × θ=0 × o ∈ {0,30,60})

**Acceptance:** 9조건 결과가 **단조 감소 경향**을 보일 것. ρ가 작아질수록, o가 커질수록 recall이 떨어져야 한다. 안 그러면 변형 구현이 잘못된 것이므로 **전체 격자로 넘어가지 말고 디버깅**한다.

변형 결과 이미지 3장을 `outputs/smoke_samples/`에 저장해 육안 확인할 수 있게 할 것.

### Phase 2 — 실험 (D+4 ~ D+7)

1. 125조건 전체 추론 → `outputs/grid_results.csv`
2. 분리형 곱셈 모델 피팅 → `curve_params.json`
3. VLM 대조 5조건

**Acceptance:** `r2_full_grid ≥ 0.80`.

### ⚠️ D+7 분기점 — 프로젝트 최대 판단 지점

| 상태 | 진로 |
|---|---|
| R² ≥ 0.80 | Phase 3 정상 진행 |
| R² < 0.80 또는 곡선 불안정 | **폴백 발동** |

**폴백 절차:** `config.ENABLE_OPTIMIZATION = False`로 전환. `optimize.py`를 실행하지 않고, **단일 배치(기존 배치 가정) 진단만** 산출한다.

> 이 현장의 기존 CCTV 배치에서 위험가중 안전모 검출률은 __%다. 고위험 구역 __개 중 __곳이 검출 임계 미만이다.

"개선하면 얼마나 좋아지는가"는 빠지지만 "지금 얼마나 못 보고 있는가"는 남는다. 최적화 구현이 통째로 빠지므로 나흘이 남고, 그 시간은 목업과 제안서에 쓴다. **폴백은 실패가 아니라 설계된 경로다.** 사용자에게 반드시 보고하고 승인을 받은 뒤 전환할 것.

### Phase 3 — 현장 모델 + 최적화 (D+8 ~ D+11)

1. `site.py` — 가상 현장 생성
2. `geometry.py` — 광선투사
3. `aggregate.py` — P_total, WDR
4. `optimize.py` — 탐욕 2회, 동일 자로 교차 측정

**Acceptance:** `comparison.json`에 두 WDR 값이 실제 계산 결과로 존재. §2의 목표 문장 빈칸이 채워짐.

### Phase 4 — 목업 (D+12 ~ D+13)

**목업을 먼저 확정하고 제안서를 쓴다** (사용자 작업 순서).

`mockup/index.html` 요구사항:

- **단일 HTML 파일.** 빌드 스텝·번들러·프레임워크 없음
- `data.json`을 `fetch`로 읽어 렌더링. 데이터를 HTML에 하드코딩하지 말 것
- **localStorage / sessionStorage 사용 금지**
- 화면 구성 4개
  1. 현장 평면도 위 **검출확률 히트맵** (복셀 격자, 0~1 색상 스케일)
  2. **미달구역 목록** — 위치, 위험가중치, P_total, 원인(ρ 부족 / θ 과다 / 가림)
  3. **WDR 요약 카드** — 기하 기준 vs 확률 기준 비교
  4. 카메라 아이콘 + 화각 부채꼴 오버레이
- 톤: LH 심사 화면. 장식 최소, 수치 가독성 우선

**Acceptance:** 브라우저에서 파일 열었을 때 에러 없이 렌더링. 콘솔 경고 0.

### Phase 5 — 예비 (D+14)

**비워둔다.** D+13 저녁에 전체 1회 통독.

---

## 8. 병행 트랙

| 트랙 | 기간 | 담당 | 비고 |
|---|---|---|---|
| A. 논거 수집 | **완료** | — | 더 파지 않음 |
| B. 실험·구현 | D+1~D+11 | Claude Code | 막히기 쉬움. D+7 분기점 |
| C. 제안서 집필 | D+1~D+14 | 사용자 | **B가 막힐 때의 대피처** |
| D. 목업 | D+12~D+13 | Claude Code | C보다 먼저 확정 |

제안서 1장(문제 제기)은 실험 결과와 무관하게 이미 집필 가능하다. Phase 1에서 환경 문제로 막히면 그 시간을 C에 쓰도록 사용자에게 알릴 것.

---

## 9. 알려진 한계 — 코드로 해결하려 하지 말 것

제안서에 선제적으로 기술할 항목이다. 보정 로직을 넣으면 오히려 검증 불가능한 코드가 된다.

- 다운샘플링은 실제 원거리 촬영과 다르다. 대기 흐림, 렌즈 광학 한계 미반영
- 호모그래피는 3D 시점 변화의 근사
- 검출기가 **SHWD 파인튜닝본**이며 A-Eye의 실제 모델(VLM 기반)이 아니다. **절대 수치가 아니라 "설치 조건에 따라 검출률이 변한다"는 관계의 존재를 증명하는 실험**으로 포지셔닝
- 분리형 곱셈 모델은 축 간 상호작용을 무시한 1차 근사.
  **잔차가 낙관 쪽으로 기운다** — 실측 0.5 미만 조건에서 평균 +0.09 높게 본다
  (`h(o)=exp(-λo)` 가 고가림에서 충분히 빨리 안 떨어진다). 안전 판정에서는
  "안 보이는 곳을 보인다고 말하는" 방향이므로 제안서에 명시한다
- **세 축 모두 측정 범위 밖으로 외삽하지 않는다.** ρ < 4px · θ > 75° · o > 0.75 는 P=0 으로 본다. 실제로 잘린 양은 가시 쌍 9,720 중 610(6.28%)이고, 그중 ρ 하한에 걸린 것은 **0개**다 — ρ 를 8·6·4 로 넓힌 조치로 그 구멍은 닫혔다
- 건설현장 실측 가림률 통계는 공개된 것이 없다. 그래서 가림률을 **입력이 아니라 스윕 파라미터**로 다룬다
- **결과를 지배하는 자유 파라미터가 둘 있다** — 스트라이프 주기(`transforms.DEFAULT_STRIPE_DIVISOR`)와 비계 시야 점유율(`config.SCAFFOLD_COVERAGE`). 둘 다 공개 근거가 없다. `src/sensitivity.py` 로 스윕한 결과 **절대 WDR 은 0.486~0.802 로 크게 흔들리지만 ΔWDR 은 +0.0249~+0.0356 으로 부호가 유지된다.** 절대 수치가 아니라 두 배치의 차이를 주장하는 근거가 여기 있다
- **DORI(IEC 62676-4)는 인간 관찰자 기준**이며 AI 검출기에 대해 검증된 바 없다. §5.4 에서 기존 방식의 기준선을 세우는 용도로만 쓴다. 픽셀밀도 기준 자체가 새롭다고 주장하지 않는다 — 새로운 것은 임계가 아니라 **연속 확률**로 다루고 다중 카메라 결합까지 간다는 점이다
- **카메라 부각(pitch)을 검사하지 않는다.** 수직 화각은 작업면을 덮도록 조준했다고 본다. 낙관 방향이다

---

## 10. 첫 실행 지시

```
Phase 0을 실행하라.
- aicctv-eval 스캐폴딩 생성
- requirements.txt: ultralytics, opencv-python, numpy, scipy, pandas, matplotlib
- config.py: §5.1, §5.2의 상수 전부 + ENABLE_OPTIMIZATION 스위치
- PROGRESS.md 생성
- SHWD 다운로드 경로 안내 출력

완료 후 Acceptance 확인 결과를 보고하고 멈출 것.
Phase 1은 별도 지시 후 시작한다.
```
