# ADDENDUM-01 — 선행연구 조사 결과 및 재포지셔닝

> `CLAUDE.md`의 부록. **먼저 `CLAUDE.md`를 읽고 이 문서를 읽을 것.**
> 이 문서는 프로젝트의 **참신성 주장 범위**를 재정의한다. 구현 스펙은 대부분 그대로지만, **§5에 명시된 4개 변경사항은 반드시 반영**해야 한다.

---

## 1. 무슨 일이 있었나

`CLAUDE.md` 작성 후 인접 분야(방재·보안공학·스포츠 중계·무선 시각센서 네트워크) 선행연구를 조사했다. 결과: **핵심 방법론의 상당 부분이 2012~2020년에 이미 확립되어 있었다.**

프로젝트는 폐기하지 않는다. 다만 **"새 모델을 제안한다"에서 "확립된 모델의 미측정 파라미터를 실측하고 제도에 접목한다"로 주장 범위를 좁힌다.**

---

## 2. 확인된 선행연구

### 2.1 직접 충돌 — 참신성 주장 불가

| 문헌 | 내용 | 우리 항목과의 충돌 |
|---|---|---|
| **IEEE, ~2012** — Probabilistic Sensing Model for Sensor Placement Optimization Based on Line-of-Sight Coverage | 가시선 기반 센서(카메라)용 확률적 감지 모델. **감지 거리 + 감지 각도 멤버십 함수**, 지형 반영. SA / L-BFGS / CMA-ES로 배치 최적화 | ρ·θ 2축 감쇠 모델 = 기존 |
| **Sensors 14(8) 15525 (2014)** — Efficient Sensor Placement Optimization Using Gradient Descent and Probabilistic Coverage | 이진 0/1 커버리지의 한계를 지적하고 확률 커버리지 도입. **"확률적 커버리지 값은 주어진 패턴 인식 시스템으로 객체를 검출할 확률로 해석 가능"**, **"센서 거동에 대한 실험적 관측(예: 거리에 따른 객체 인식률)으로 추정 가능"**이라고 명시 | **우리 실험이 이 논문의 후속과제로 이미 기술되어 있음** ⚠️ 최우선 확인 대상 |
| **WSN target coverage 문헌 다수** | 확률 센서 협조로 검출확률 상승. `1−Π(1−P)` 결합은 표준 | 곱셈 보수 결합 = 기존 |
| **RESPIRE (UCSD, 2020)** | 최대 커버리지 접근은 성긴 배치를 낳아 대부분 영역이 단일 센서로만 덮임. 검출 가능도를 고려하면 견고성 최대 50% 향상 | **"중첩이 성능을 올린다"는 반전 = 기존** |

### 2.2 위협 없음

- **스포츠 중계 카메라 배치** — 미학·앵글 전환 문제. CV 연구는 전부 추적 알고리즘 쪽. 배치 설계로 환류시키는 연구 없음
- **방재·재난 감시** — 지형 기반 센서 배치는 2.1과 같은 계열

### 2.3 건설 도메인 (기존 조사, 유효)

- 카메라 배치 최적화 리뷰 (MDPI Buildings 14-12-3930, 2024) — 분야 성숙
- 4D BIM CCTV 계획 (SCI4D 2021 / ASCE JCEM 2024)
- 이설비용·위험가중 MILP (2025.10 ScienceDirect) — **4D 제외 사유**
- 복셀 커버리지·중첩 (MDPI Buildings 14-6-1880, 2024)
- 건설현장 가림률 실측 통계: **공개 데이터 없음.** MOCS(41,668장), SODA(19,846장) 모두 가림 등급 어노테이션 부재

---

## 3. 기존 / 신규 분해 — 확정판

| # | 구성요소 | 판정 |
|---|---|---|
| 1 | 육안 인식거리 ≠ AI 검출거리 | 기존 |
| 2 | 확률적 커버리지 모델 틀 | 기존 |
| 3 | 거리·각도 2축 감쇠 | 기존 |
| 4 | 곱셈 보수 결합 `1−Π(1−P)` | 기존 |
| 5 | 중첩이 검출률을 올린다 | 기존 |
| 6 | submodular 탐욕 (1−1/e) | 기존 |
| 7 | 위험가중 목적함수 | 기존 |
| 8 | BIM 기반 CCTV 배치 | 기존 |
| 9 | 헬멧 검출·가림 성능 저하 | 기존 |
| **10** | **가림률을 연속 독립축으로 실측** | **신규 가능** (기존은 이진 LOS) |
| **11** | **특정 딥러닝 검출기 응답면 실측 → 파라미터 대입** | **신규 가능 ⚠️미확인** |
| **12** | **탐지 항목별 곡선 분리 + 최솟값 집계** | **신규 유력** |
| **13** | **인증 성능 ≠ 현장 발현율 제도 논증** | **신규** |
| **14** | **국내 심사 절차 접목** | **신규** |

**9 대 5. 신규 5개 중 2개 미확인.**

---

## 4. 재포지셔닝 — 프로젝트 정의문 교체

기존 정의문(`CLAUDE.md` §1)은 유효하되, 참신성 서술은 아래로 **완전히 대체**한다.

> 확률적 커버리지 기반 카메라 배치는 시각센서 네트워크 분야에서 확립된 방법론이다. 다만 기존 연구는 감쇠 함수를 해석적으로 가정하였고(멤버십 함수, 지수 감쇠), 실제 검출기의 응답을 측정하여 대입한 사례는 확인되지 않았다.
>
> 본 제안이 새로 하는 것은 셋이다.
> **(1)** 감쇠 함수를 실측으로 대체한다.
> **(2)** 탐지 항목별로 함수가 다르다는 점을 반영한다.
> **(3)** 그 결과를 국내 스마트 안전기술 심사 척도로 접목한다.

### 금지 표현

산출물(코드 주석·README·JSON·목업 텍스트) 어디에도 다음을 쓰지 않는다.

- "최초", "novel", "unprecedented", "기존에 없던"
- "기존 연구는 중첩을 낭비로 본다" → **사실이 아니다** (RESPIRE 2020)
- "확률적 커버리지 모델을 제안한다" → 제안하는 게 아니라 **파라미터를 채운다**

---

## 5. 구현 변경사항 4개 — 반드시 반영

`CLAUDE.md`의 파이프라인·격자·데이터 계약은 **그대로 유효**하다. 아래만 수정한다.

### 5.1 §5.3 주석 수정 (사실 오류)

현재 `CLAUDE.md` §5.3에 "기존 연구(MDPI 2024)는 중첩 최소화를 목표로 하는데 우리는 반대"라는 취지의 주석 지시가 있다. **센서 네트워크 분야에서는 이미 알려진 결과이므로 이 서술을 삭제**한다.

**코드 동작은 변경 없음.** 중첩을 페널티로 다루지 않는다는 지시는 그대로 유효하다. 바뀌는 것은 주석 문구뿐이다.

수정 후 주석:
```python
# 다중 카메라 결합. WSN target coverage 문헌의 표준 형태.
# 중첩은 페널티가 아니다. 각 0.6인 두 대가 합쳐 0.84가 된다.
# 참고: RESPIRE(2020)도 coverage-only 접근이 단일 센서 의존을 낳는다고 보고.
```

### 5.2 가림률 축 우선순위 상향 ★

**가림률 `o`가 이제 실험에서 가장 가치 있는 축이다.** 기존 확률 커버리지 문헌은 가림을 이진(막히면 0)으로 처리하거나 지형 가시성으로만 다룬다. 연속 축으로 실측한 사례가 확인되지 않았다.

따라서:

- Phase 1 스모크 테스트 9조건은 **ρ와 o를 우선** 배치한다 (θ는 0 고정). `CLAUDE.md` §7 Phase 1의 구성과 동일하므로 변경 없음
- 시간이 부족해 격자를 줄여야 할 경우, **θ 수준을 5→3으로 줄이고 o 수준 5개는 유지**한다
- `outputs/smoke_samples/`에 저장하는 변형 예시 이미지에 **가림 마스킹 결과를 반드시 포함**한다. 수직 스트라이프가 비계처럼 보이는지 육안 확인이 필요하다

### 5.3 항목별 분해는 축소 불가 ★★

`CLAUDE.md`는 D+7 시간 부족 시 "안전모 1종으로 되돌린다"는 폴백을 허용했다. **이 폴백을 철회한다.**

항목별 곡선 분리(#12)가 **현재 가장 유력한 신규 항목**이다. 이것을 버리면 남는 신규성이 제도 논증(#13, #14)뿐이 되고, 실험 파트의 존재 이유가 사라진다.

- 4개 탐지 항목(person / helmet / pose-fallen / pose-gesture)은 **어떤 경우에도 유지**한다
- 축소가 불가피하면 **항목이 아니라 격자 해상도를 줄인다** (5수준 → 3수준)
- `aggregate.py`의 종합 지표는 **항목별 최솟값**이다. 평균으로 바꾸지 않는다. 안전모 실패가 사람 검출 성공에 가려지면 이 프로젝트의 논지가 사라진다

### 5.4 [신규] 가정 곡선 vs 실측 곡선 비교 ★★★

**이것을 추가한다. 비용 대비 효과가 가장 크다.**

"기존 연구의 감쇠 함수는 가정값이었다"는 주장을 **말이 아니라 그림으로** 증명한다.

구현:

1. `src/baseline_curve.py` — 선행연구 방식의 **해석적 가정 곡선**을 하드코딩한다
   - 형태: `P(d) = exp(-β·d)` 또는 로지스틱. `α`(50% 지점)는 **DORI 표준의 Recognise 기준 125 px/m**에서 유도
   - 이 값은 문헌 관행에서 온 것이며 측정값이 아님을 주석에 명시
2. `optimize.py`를 **3회** 실행한다
   - A. 기하 커버리지 기준
   - B. **가정 곡선** 기준
   - C. **실측 곡선** 기준
3. 세 배치를 모두 **C의 자(실측 곡선)로 재측정**하여 WDR을 비교한다
4. `outputs/comparison.json`에 3개 항목으로 기록

기대 산출 문장 (수치는 실행 후 채움):

> 기하 커버리지 기준 배치의 실측 WDR은 __%, 문헌의 가정 곡선으로 설계한 배치는 __%, 실측 곡선으로 설계한 배치는 __%였다. **가정 곡선을 쓰면 기하 기준보다는 나아지지만 실측 기준에는 미치지 못한다.**

이 세 번째 막대가 있어야 "실측이 왜 필요한가"가 증명된다. 없으면 우리 기여는 주장에 머문다.

**추가 비용:** 탐욕 최적화 1회 실행분. 구조 변경 없음.

`comparison.json` 계약:

```json
{
  "measured_with": "empirical_curve",
  "placements": {
    "geometric":  {"camera_ids": [], "WDR": null},
    "assumed":    {"camera_ids": [], "WDR": null, "curve_source": "DORI_recognise_125ppm"},
    "empirical":  {"camera_ids": [], "WDR": null}
  },
  "per_target_WDR": {
    "geometric": {"person": null, "helmet": null, "pose_fallen": null, "pose_gesture": null},
    "assumed":   {"person": null, "helmet": null, "pose_fallen": null, "pose_gesture": null},
    "empirical": {"person": null, "helmet": null, "pose_fallen": null, "pose_gesture": null}
  },
  "status": "not_computed"
}
```

### 5.5 목업 반영

`mockup/index.html`의 WDR 요약 카드를 **2단 비교에서 3단 비교로** 확장한다. 항목별 토글 4개는 `CLAUDE.md` 명세 그대로 유지한다.

---

## 6. 미확인 리스크 3건 — 사용자 담당

에이전트가 처리할 수 없다. 사용자가 원문을 확인해야 하며, 결과에 따라 서술이 바뀐다.

| 우선순위 | 문헌 | 확인 사항 | 실패 시 영향 |
|---|---|---|---|
| **1** | Sensors 14(8) 15525 (2014) | 인식률을 **실제로 측정**했는지, 언급만 했는지 | 측정했다면 신규항목 #11 소멸. #10·#12·#13·#14만 남음 |
| 2 | Automation in Construction (2024) — Image hashing-based shallow object tracker, scaffolding scenes | 가림률·시점 기반 신뢰도 모델링이 **사전 배치 설계**인지 **사후 추적 신뢰도**인지 | 사전 설계라면 #10 소멸 |
| 3 | IEEE 확률적 감지 모델 (~2012) | 각도 멤버십 함수의 근거가 **실측인지 가정인지** | 실측이라면 #11 약화 |

**#11이 소멸해도 프로젝트는 성립한다.** #12(항목별 분해)와 #13·#14(제도 접목)가 주력이 된다. 다만 제안서 서술이 다시 바뀌므로 **구현을 시작하기 전에 확인**하는 것이 좋다.

에이전트는 이 확인 결과를 기다리지 말고 Phase 0~1을 진행한다. 구현 스펙은 세 결과 어느 쪽에서도 동일하다.

---

## 7. 프로젝트 가치 평가 (판단 근거 공유용)

에이전트가 우선순위를 판단할 때 참고할 것. 코드에는 반영하지 않는다.

**대회 심사 구성:** 외부위원 4인 + 내부위원 4인, 사전심사와 본심사 2단계. **배점표는 공개 자료에 없음.**

| 축 | 등급 | 비고 |
|---|---|---|
| 발주처 문제 정합성 | **A+** | LH 자체 문서에 "운용 방식 조정 필요"로 기록됨 |
| 적용성·파급효과 | **A** | 추가 제출물 0, 기존 심사 절차에 접속. 대상 1,122개 현장 |
| 심사자 전달력 | **A** | 픽셀 산수 표가 전공 무관하게 이해됨 |
| 창의성 | **C+** | 위 §3 분해표대로 |
| 기술적 완성도·실증 | **현재 D** | 실측치 미확보. 곡선이 나오면 B |

**종합: 조건부 A.** 승부는 창의성이 아니라 정합성 × 적용성이며, **실증 D를 B로 올리는 것이 유일한 병목**이다.

즉 **에이전트의 작업(Phase 2 곡선 확보, Phase 3 세 배치 비교)이 이 프로젝트의 등급을 결정한다.** 다른 모든 요소는 이미 확보되어 있다.

**불리한 신호:** 제14회 상위 수상작 2건(대상 상수관로 결함 탐지, 최우수 교량 IoT 계측)은 준공 후 영구자산 진단이었고, 안전관리+BIM 계열(4D-BIM 위험성 평가)은 장려에 그쳤다. 이를 상쇄하는 것은 타이밍이다 — A-Eye 2026.4 운영 개시, 가이드라인 2026.1 개정 진행 중.

---

## 8. 요약 — 에이전트가 기억할 5줄

1. 방법론은 기존 것이다. **우리는 파라미터를 채운다.**
2. **가림률 연속 축**과 **탐지 항목별 분해**가 실험의 존재 이유다. 둘 다 축소 금지.
3. **가정 곡선 vs 실측 곡선 비교(§5.4)를 반드시 구현**한다. 이것이 기여를 증명하는 유일한 그림이다.
4. "최초·novel" 류 표현 금지. 중첩 반전을 우리 발견으로 서술하지 말 것.
5. 실증 D→B가 프로젝트의 유일한 병목이다. **Phase 2와 Phase 3이 전부다.**
