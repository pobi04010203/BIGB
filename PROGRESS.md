# PROGRESS — AI CCTV 배치 적정성 평가 모델

Phase 별 실행 결과 로그. **실제로 실행한 것만 적는다.**
계산하지 않은 값은 쓰지 않고 `미산출` 로 남긴다 (CLAUDE.md §0.1-1).

| Phase | 기간 | 상태 |
|---|---|---|
| 0 스캐폴딩 | D+0 | **완료** |
| 1 파이프라인 | D+1 ~ D+3 | **완료** — Acceptance 통과 |
| 2 실험 | D+4 ~ D+7 | **완료** — 288조건 · R² 0.8977, Acceptance 통과 |
| 3 현장 모델 + 최적화 | D+8 ~ D+11 | **완료** |
| 4 목업 | D+12 ~ D+13 | **완료** |
| 5 예비 | D+14 | **완료** — 통독·계약 점검 |
| 보강 강건성 | 2026-08-15 | **완료** — 기준선·외삽 가드·민감도 |
| 부피 복셀화 + 가정 곡선 | 2026-08-19 | **완료** — 복셀 1,256 → 9,876 · 배치 3종 |
| 검출기 yolo26 전환 | 2026-08-19 | **보류** — 설정만 앞서 있어 되돌림 |

> **수치의 정본은 맨 아래 「부피 복셀화 이후」 절이다 (2026-08-19).** 그 위의
> 「강건성 보강」 절은 복셀이 작업면 한 층(1,256)이던 시절의 기록이고, 다시 그 위의
> Phase 2~5 절은 격자가 288조건이 되기 전의 기록이다. **앞 절들은 고치지 않고**
> 그때까지의 실행 기록으로 남긴다 — 언제 무엇이 바뀌었는지가 로그의 값어치다.

---

## Phase 0 — 스캐폴딩 (완료)

### 만든 것

```
├─ CLAUDE.md            (사용자 작성)
├─ PROGRESS.md          이 문서
├─ requirements.txt     ultralytics · opencv-python · numpy · scipy · pandas · matplotlib
├─ config.py            §4 격자 + §5.1 현장 + §5.2 기하 + ENABLE_OPTIMIZATION
├─ .gitignore           data/raw · data/filtered · *.pt 제외
├─ data/{raw,filtered}/
├─ notebooks/
├─ src/
├─ outputs/smoke_samples/
└─ mockup/
```

`src/` 는 비어 있다. 모듈 구현은 Phase 1 부터다.

### Acceptance — 통과

```
python -c "import ultralytics, cv2, numpy, scipy"   → PASS
```

실행일 2026-08-14. 설치된 버전:

| 패키지 | 버전 |
|---|---|
| ultralytics | 8.4.120 |
| opencv-python | 5.0.0 |
| numpy | 2.2.6 |
| scipy | 1.15.3 |
| pandas | 2.3.3 |
| matplotlib | 3.10.8 |
| torch | 2.7.1+cu118 |

`config.py` 로드 확인:

| 값 | 결과 |
|---|---|
| `FOCAL_PX` | 1920.00 — `(3840/2) / tan(45°)` |
| `N_CONDITIONS` | 125 |
| `rho_px(38.2)` | 12.57 px |
| `ENABLE_OPTIMIZATION` | True |

### 환경에서 확인된 것 — Phase 1 전에 판단 필요

1. **`torch` 가 CUDA 빌드(cu118)이고 `torch.cuda.is_available()` 이 True 다.**
   이 세션에서 설치한 것이 아니라 기존에 깔려 있던 것이다. CLAUDE.md §0.1-3 은
   로컬 CUDA **세팅**을 금지하므로 손대지 않았다. 추론은 `device='cpu'` 를
   명시하거나 Colab 을 쓰면 되고, 환경 조정에 시간을 쓰지 않는다.

2. **`opencv-python` 이 4.13.0 → 5.0.0 으로 올라갔다.** `ultralytics` 설치 과정의
   의존성 해결 결과다. 쓰는 API 는 `resize` · `warpPerspective` · `rectangle`
   정도라 영향이 없을 것으로 보이나, Phase 1 스모크 테스트에서 실제로 확인한다.

3. **CLAUDE.md §5.2 의 출력 예시가 자체 공식과 맞지 않는다.**
   예시는 `d_m: 38.2, rho_px: 27.4` 인데, 같은 절의 공식
   `ρ = f_px · H_head / d` 에 4K·HFOV 90°·H_head 0.25m 를 넣으면 **12.57** 이다.
   27.4 가 나오려면 `f_px ≈ 4187` (HFOV 약 49°) 이어야 한다.
   예시 JSON 은 형식을 보이려는 것으로 보고 **공식을 정본으로 삼았다.**
   검산 기준값으로 27.4 를 쓰지 말 것. 의도가 다르면 알려주면 고친다.

---

## Phase 1 — 파이프라인 (부분 완료)

| # | 항목 | 상태 |
|---|---|---|
| 1 | `src/transforms.py` — ρ/θ/o 3축 변형 | **완료** |
| 2 | 데이터 필터링 (머리 bbox ≥ 40px, 목표 500장) | **완료** |
| 3 | 스모크 테스트 9조건 | **통과** |

### ① transforms.py — 완료

변형 순서는 `rho → theta → occlusion` 이다. 가림은 카메라와 피사체 사이의 전경
부재를 모사하므로 마지막에 얹는다. 앞에 두면 다운샘플·워핑에 스트라이프가 뭉개져
목표 가림률이 흐트러진다.

`apply_condition()` 은 실제로 적용된 값을 함께 돌려준다. 정수 픽셀로 떨어뜨리느라
목표와 어긋나므로 결과 CSV 에는 목표값이 아니라 **실측값**을 기록한다.

**동작 확인** (`data/raw/image/3.jpg`, 440×293). 데모 이미지에는 어노테이션이 없어
원본 머리 크기를 `48px` 로 **가정**했다 — 측정값이 아니다.

ρ — 선명도가 단조 감소한다:

| ρ (px) | 48 | 32 | 24 | 16 | 12 |
|---|---:|---:|---:|---:|---:|
| Laplacian var | 2814.7 | 294.5 | 150.7 | 59.1 | 27.7 |

가림률 — 목표 대비 실측:

| 목표 | 0% | 15% | 30% | 45% | 60% |
|---|---:|---:|---:|---:|---:|
| 실측 | 0.00% | 17.05% | 28.41% | 45.45% | 61.82% |

440px 폭에서 스트라이프 주기가 18px 라 양자화 오차가 최대 2.05%p 다. 실제 격자는
더 큰 이미지를 쓰므로 줄어들며, 어느 쪽이든 실측값을 기록하므로 오차가 지표에
섞이지 않는다.

θ — `theta=0` 은 원본과 바이트 단위로 동일(항등). 각도가 커질수록 내용 점유가
95.4% → 81.0% → 60.7% → 37.7% 로 단조 감소한다.

`outputs/smoke_samples/` 에 3장 저장: `rho48_theta0_occ0` · `rho24_theta30_occ30` ·
`rho12_theta60_occ60`. 육안 확인 결과 흐림·사다리꼴 원근·수직 스트라이프가 모두
의도대로 걸린다.

### ② 데이터 필터링 — 완료

`src/filter_data.py`. 어노테이션 7,581건을 읽어 조건 통과분을 `data/filtered/manifest.json`
에 기록한다. **이미지를 복사하지 않는다** — 1GB 를 복제할 이유가 없고 뒤 단계는
원본 경로를 그대로 읽으면 된다.

난수를 쓰지 않는다. 파일명 정렬 순서로만 골라 몇 번을 돌려도 같은 500장이 나온다.

**§4.1 에 없는 조건을 하나 넣었다 — 대표 머리크기 ≥ 48px.**
ρ 축 기준 조건이 48px 인데(§4.2) `apply_rho` 는 다운샘플만 한다. 대표 머리크기가
42px 인 이미지에 ρ=48 을 걸면 아무 변형도 안 일어나 "48px 조건"이라는 라벨이 실제와
어긋난다. §4.1 이 40px 필터의 목적을 *"기준 조건(고해상도)을 확보하기 위함"* 이라고
밝히므로 그 목적에 맞췄다.

결과:

| 항목 | 값 |
|---|---|
| 전체 이미지 | 7,581장 |
| 짧은 변 ≥ 40px 통과 | 5,995장 |
| ＋ 대표 머리크기 ≥ 48px | 4,963장 (후보) |
| **선정** | **500장** |
| 구성 | 두 클래스 공존 232 · hat만 134 · person만 134 |
| 인스턴스 | hat 899 · person 691 |
| 대표 머리크기 | 중앙값 76.0px (최소 48.0 / 최대 1114.0) |

**30px 완화는 하지 않았다.** 후보가 4,963장으로 목표 500장의 10배라 §4.1 의 완화
조항이 발동할 이유가 없다.

잡라벨 `dog` 3건은 제외했다.

### ③ 스모크 테스트 — Acceptance 통과

#### 먼저 검출기를 바꿔야 했다

§4.3 이 지정한 COCO 사전학습 YOLOv8n 에는 `hat` · `helmet` · `head` 클래스가 없다
(전수 확인). COCO 의 `person` 은 전신 박스라 SHWD 의 머리 박스와 IoU 0.5 에서
겹치지 않는다. **변형을 전혀 걸지 않은 기준 조건**에서 실측하니 주 지표가 0.088 로,
악화시켜봐야 0 으로 떨어질 뿐이라 곡선이 나오지 않았다.

사용자 승인(2026-08-14)으로 **SHWD 파인튜닝**을 추가했다. `src/train_detector.py`.

| | `recall_hat` | `recall_nohat` |
|---|---:|---:|
| COCO 사전학습 | 0.056 | 0.088 |
| **SHWD 파인튜닝** | **0.958** | **0.912** |

학습 조건 — yolov8n 출발, 50epoch, batch 16, imgsz 640, patience 15, seed 0,
RTX 3070 Ti, 29분. train 5,457장 / val 607장. **test 분할은 넣지 않았다.**

val 성적:

| Class | Images | Instances | P | R | mAP50 | mAP50-95 |
|---|---:|---:|---:|---:|---:|---:|
| all | 607 | 9,925 | 0.935 | 0.878 | **0.935** | 0.605 |
| hat | 282 | 747 | 0.944 | 0.889 | 0.938 | 0.736 |
| person | 369 | 9,178 | 0.927 | 0.868 | 0.932 | 0.475 |

SHWD 저장소가 제시한 darknet 기준선 mAP 88.5 보다 높다.

**CLAUDE.md §4.3 이 아직 "사전학습 가중치"로 적혀 있다.** 실제와 어긋나므로 고쳐야
하지만 CLAUDE.md 는 지시서라 임의로 손대지 않았다.

#### Acceptance — 통과

실험셋 500장 전체, `recall_nohat`(주 지표):

| | o=0 | o=30 | o=60 |
|---|---:|---:|---:|
| **ρ=48** | 0.9578 | 0.2389 | 0.0036 |
| **ρ=24** | 0.9534 | 0.1770 | 0.0029 |
| **ρ=12** | 0.8857 | 0.0816 | 0.0007 |

ρ 축·o 축 모두 단조 감소. `recall_hat` 과 `conf_mean` 도 같은 방향이다.

예비 실행(60장)에서는 o=0 행의 ρ=48→24 가 0.842→0.858 로 뒤집혔으나, 500장에서
사라졌다. **표본 노이즈였다.** §7 이 단조성 위반 시 디버깅을 요구하므로 전체
실험셋으로 다시 재고 판정했다.

#### 관측 — 가림의 효과가 압도적이다

30% 가림에 주 지표가 0.958 → 0.239 로 무너진다. 스트라이프 주기를 화면 가로의
1/24 로 잡아 머리(중앙값 76px)마다 1~3개가 지나간다. §4.2 는 주기를 자유
파라미터로 두었으므로 조정 여지가 있다. **이대로 두면 `h(o)` 가 곱셈 모델을
지배한다.** Phase 2 피팅 결과를 보고 판단한다.

#### 고친 것 — n_fp 집계

첫 실행에서 최적 조건 n_fp 가 4,503 으로 GT 1,907 을 넘었다. 검출기 오류가 아니라,
매니페스트가 40px 이상만 GT 로 들고 있어 **검출기가 작은 머리를 맞게 찾아낸 것이
전부 FP 로 집계**된 탓이다. 매니페스트에 `all_instances`(크기 무관 전량)를 추가하고,
거기 걸리는 검출은 FP 에서 뺐다. FP/GT 비가 2.4 → 0.3~0.4 로 정상화됐다.

`recall` 은 GT 만 쓰므로 위 Acceptance 판정에는 영향이 없다.

---

## (해소됨) SHWD 데이터셋 수급

첫 zip (`Safety-Helmet-Wearing-Dataset-master.zip`, 3.6MB) 은 GitHub 저장소 소스라
데모 이미지 21장에 어노테이션 0개였다. 이어서 받은 `VOC2028.zip` (1.12GB) 이
실제 데이터셋이었고, 이미지 7,581 / 어노테이션 7,581 로 확인됐다. 두 zip 모두
사용자 승인 하에 삭제했다.

데이터 구성 (실측):

| 클래스 | 인스턴스 |
|---|---:|
| `person` (미착용) | 111,514 |
| `hat` (착용) | 9,044 |
| `dog` (잡라벨) | 3 |

분할은 train 5,457 / val 607 / trainval 6,064 / test 1,517 로 이미 나뉘어 있다.
전체 인스턴스의 짧은 변 중앙값은 21.0px 이고, 40px 이상은 20.6% 다.

②는 이 데이터로 완료했다. 남은 것은 ③이며 위의 검출기 문제에 걸려 있다.

---

## Phase 2 — 실험 (완료)

### 125조건 전체 추론

`python src/run_grid.py` → `outputs/grid_results.csv`. 실험셋 500장(test 분할),
IoU 0.5 · conf 0.25 고정.

축별 단면, 주 지표 `recall_nohat`:

| ρ (θ=0, o=0) | 48 | 32 | 24 | 16 | 12 |
|---|---:|---:|---:|---:|---:|
| | 0.9578 | 0.9570 | 0.9534 | 0.9308 | 0.8857 |

| θ (ρ=48, o=0) | 0 | 15 | 30 | 45 | 60 |
|---|---:|---:|---:|---:|---:|
| | 0.9578 | 0.9592 | 0.9395 | 0.9046 | 0.5623 |

| o (ρ=48, θ=0) | 0 | 15 | 30 | 45 | 60 |
|---|---:|---:|---:|---:|---:|
| | 0.9578 | 0.6737 | 0.2389 | 0.0561 | 0.0036 |

**세 축의 세기가 크게 다르다.** 실험 범위 안에서 ρ 는 7%p, θ 는 40%p, o 는 95%p 를
움직인다. 가림이 압도적이다.

### 곡선 피팅 — Acceptance 통과

`python src/fit_curve.py` → `outputs/curve_params.json`.

| 항목 | 값 | 단면 R² |
|---|---|---:|
| `f(ρ)` 로지스틱 | L=0.9574 · k=0.2578 · x0=2.2484 | 0.9999 |
| `g(θ)` 2차 | a=+0.006609 · b=−0.00021463 | 0.9128 |
| `h(o)` 지수감쇠 | λ=4.1150 | 0.9380 |

**전체 125점 R² = 0.9185** (기준 0.80). §7 D+7 분기점에서 **정상 진행** 경로다.
`ENABLE_OPTIMIZATION` 은 True 로 둔다.

`g(θ)` 는 2차와 지수 중 R² 가 높은 쪽을 골랐다(2차 0.9128 > 지수 0.8846).
가림은 목표값이 아니라 **실측 가림률**로 피팅했다.

### Phase 3 전에 판단이 필요한 것 셋

R² 는 통과했으나 잔차의 **방향**에 문제가 있다. 셋 다 같은 쪽으로 기운다 —
**안 보이는 곳을 보인다고 말한다.** 안전 판정 도구에서 위험한 방향이다.

#### 1. 저구간 낙관 편향

| 실측 구간 | 조건 수 | 평균 잔차 (예측−실측) |
|---|---:|---:|
| 0.0 ~ 0.1 | 58 | **+0.0912** |
| 0.1 ~ 0.5 | 31 | **+0.0989** |
| 0.5 ~ 0.9 | 23 | −0.0609 |
| 0.9 ~ 1.0 | 13 | +0.0161 |

125점 중 89점이 실측 0.5 미만인데 거기서 모델이 평균 0.09 이상 높게 본다.
`h(o) = exp(-λ·o)` 가 고가림에서 충분히 빨리 떨어지지 않는 탓이다. 실측 비율은
o=0.60 에서 0.0038 인데 모델은 0.0846 을 준다.

#### 2. `g(θ)` 가 단조 감소가 아니다

§4.5 는 *"1에서 시작하는 단조 감소"* 를 요구하는데 피팅된 2차는 15.4° 에서
꼭짓점을 갖는다.

| θ | 0 | 15 | 30 | 45 | 60 |
|---|---:|---:|---:|---:|---:|
| `g(θ)` | 1.0000 | **1.0508** | 1.0051 | 0.8628 | 0.6239 |

원인은 실측 자체다 — θ=15 (0.9592) 가 θ=0 (0.9578) 보다 근소하게 높다(+0.0014).
2차가 이 노이즈를 5% 융기로 증폭했다. 이대로 최적화에 넣으면 **15° 내리깐 카메라를
정면보다 선호**한다. 물리적으로 말이 안 되는 건 아니지만 스펙과 어긋난다.

#### 3. `f(ρ)` 가 실험 범위 밖에서 외삽된다

실험은 ρ ∈ [12, 48] 만 덮는데, 로지스틱 중점이 x0=2.25 라 그 범위에서는 거의
포화 상태다. 범위를 벗어나면:

| ρ (px) | 48 | 24 | 12 | 8 | 5 | 3 |
|---|---:|---:|---:|---:|---:|---:|
| `f(ρ)` | 0.9574 | 0.9539 | 0.8858 | 0.7803 | 0.6417 | 0.5250 |
| | 측정 | 측정 | 측정 | **외삽** | **외삽** | **외삽** |

**Phase 3 에서 이게 문제가 된다.** §5.2 의 4K·HFOV 90° 에서 `ρ = 480/d` 이므로
ρ=12px 는 d=40m 다. 현장이 100×60m 라 **대부분의 복셀-카메라 쌍이 40m 를 넘고**,
그 구간은 측정한 적이 없다. ρ=5px(d=96m) 에서 0.64 라는 값은 데이터가 뒷받침하지
않으며 실제보다 크게 높을 것이 거의 확실하다.

Phase 3 를 이 상태로 돌리면 WDR 이 전반적으로 부풀려진다.

---

## Phase 2 보강 — ρ 격자 확장 (승인 후)

원안 125조건에 ρ = 8·6·4px 를 더해 **200조건**으로 늘렸다. 외삽 구간을 실측으로
덮기 위해서다. `run_grid.py` 에 재개 기능을 넣어 기존 125조건은 다시 돌리지 않았다.

새로 잰 값 (θ=0, o=0):

| ρ (px) | 12 | 8 | 6 | 4 |
|---|---:|---:|---:|---:|
| `recall_nohat` | 0.8857 | 0.7669 | 0.5462 | 0.1792 |

**외삽이 실제로 낙관적이었다.** 확장 전 곡선은 ρ=5px 에서 0.64 를 예측했는데,
실측은 ρ=6 에서 0.546, ρ=4 에서 0.179 다. 로지스틱 중점도 x0 = 2.25 → **5.73** 으로
제자리를 찾았다(k = 0.2578 → 0.7385).

재피팅 결과: f 단면 R² 0.9907 · g 0.7769 · h 0.9380 · **전체 200점 R² 0.8731**.
저ρ 구간이 분리형 곱셈 모델로는 덜 맞아 R² 는 0.9103 → 0.8731 로 내려갔으나 기준은 통과한다.

`g(θ)` 에는 `a ≤ 0, b ≤ 0` 제약을 걸어 15.4° 꼭짓점을 없앴다. 단면 R² 가
0.9128 → 0.7769 로 내려가지만 **최적화가 15° 내리깐 카메라를 정면보다 선호하는**
결함이 사라진다.

---

## Phase 3 — 현장 모델 + 최적화 (완료)

### 가상 현장

100×60m · 복셀 2m · 작업면 z=0.5~2.0m. 복셀 **1,256개**(골조 내부 제외), Σw **2,032**.
카메라 후보 24 (경계 폴 16 · 코어 상부 4 · 타워크레인 4), 예산 8.

### 스펙이 비운 자리를 채운 것 둘 — 둘 다 결함을 먼저 만들었다

**1. 비계를 속 찬 벽으로 두면 현장이 안 보인다.**
처음 모델은 완전 차폐 **57%** 였다. 실제 비계는 지주·띠장 사이가 비어 있고,
그 부분 가림은 실험의 `o` 축이 바로 다루는 대상이다. `Box.coverage` 를 넣어
점유율 0.35 로 넘겼다 → 가시 9.3% → 26.9%.

**2. 카메라 지향을 "전부 현장 중심" 으로 두면 내부 카메라가 죽는다.**
§5.2 는 HFOV 를 f_px 산출에만 쓰고 지향을 정하지 않았다. 중심을 보게 하니
코어 상부·타워크레인 카메라가 **서로를 쳐다봐** 쓸모없어졌고, 최적화가 경계 폴만
8대 골랐다. 지금은 카메라마다 **자기 위험가중 가시량을 최대로 만드는 방위**를
15° 간격 24방위 전수 탐색으로 고른다. 기하 배치가 타워크레인을 고르기 시작했다.

가시 **32.2%** · 거리 중앙값 42.3m · ρ 중앙값 11.3px · **ρ 최소 4.9px 로 실측 범위 안**.

### 결과 — §2 목표 문장이 채워졌다

| 기준 | WDR | 미달구역 | 선정 |
|---|---:|---:|---|
| 기하 커버리지 (기존) | **61.0%** | 414 | c_b00, c_b09, c_b01, c_t21, c_b05, c_b15, c_b10, c_t22 |
| 검출확률 (제안) | **70.6%** | 162 | c_b08, c_b01, c_b00, c_b09, c_b02, c_b07, c_b14, c_b11 |

ΔWDR **+9.6%p** · 미달구역 **-252개** · 공통 카메라 3대.

**두 배치를 모두 검출확률의 자로 쟀다.** 기존 기준으로 고른 배치도 실제 검출률은
낮다는 것이 §5.4 의 확인 대상이었고, 같은 8대로 재배치하니 미달구역이 61% 줄었다.

---

## Phase 4 — 목업 (완료)

`mockup/index.html` — 단일 HTML, 빌드 스텝·번들러·프레임워크 없음.
`data.json` 을 `fetch` 로 읽고 localStorage/sessionStorage 를 쓰지 않는다.

화면 4개: 검출확률 히트맵(복셀 격자 + 카메라 + 화각 부채꼴) · 미달구역 목록
(위치·가중치·P_total·원인) · WDR 요약 카드 · 배치 비교표.

**Acceptance — 통과.** 헤드리스 크롬으로 렌더 확인, **콘솔 에러 0 · 경고 0**.
처음엔 favicon 404 하나가 떴는데 인라인 SVG 파비콘으로 없앴다.

---

## Phase 5 — 통독 (완료)

전 파이프라인을 다시 돌려 **바이트 단위로 같은 결과**가 나오는 것을 확인했다.
난수를 쓰지 않으므로 재현된다.

산출물 계약 12항목 전부 통과:

- `curve_params.json` status ok · R² 0.8731 ≥ 0.80 · 조건 200 · 이미지 500 · null 없음
- `site_eval.json` status ok · WDR 전부 실측값 · 복셀 1,256
- `comparison.json` 두 WDR 실재 (§7 Phase 3 Acceptance)
- `mockup/data.json` == `site_eval.json`
- `grid_results.csv` 200행 · 빈 지표 없음

### 남은 한계 — 제안서에 선제 기술할 것

1. **모델이 낙관 쪽으로 기운다.** 실측 0.5 미만 구간에서 평균 +0.09 과대예측.
   `h(o)=exp(-λo)` 가 고가림에서 충분히 빨리 안 떨어진다. 안전 판정에서
   "안 보이는 곳을 보인다고 말하는" 방향이다.
2. **카메라 부각(pitch)을 검사하지 않는다.** 수직 화각은 작업면을 덮도록
   조준했다고 본다.
3. **비계 점유율 0.35 는 잠정값**이다. 공개 통계가 없다.
4. **위험 가중치 1~5 는 상대 순위 표현**이며 LH 지수 원자료가 아니다.
   `RISK_WEIGHT_SOURCE = "hardcoded_provisional"` 로 표시해 두었다.
5. 다운샘플링·호모그래피·분리형 곱셈 모델의 근사 한계는 CLAUDE.md §9 그대로다.

---

## 강건성 보강 (2026-08-15)

선행기술 조사에서 나온 지적 여섯을 반영했다. **결론의 부호는 유지되고, 크기는
줄었다.** 줄어든 쪽이 정직한 값이다.

### 1. 기하 기준선에 DORI 최소 픽셀밀도를 걸었다

기존 `coverage_score` 는 "화각 안 + 완전차폐 아님" 만 봤다. 그러면 116m 떨어진
복셀도 커버로 잡힌다. 실무 설계도구(JVSG · Axis Site Designer)는 IEC 62676-4 의
DORI 최소 PPM 을 지키므로, **기준선을 그 수준으로 올려야 비교가 성립한다.**
안 그러면 기존 방식을 실무보다 못하게 모델링해놓고 이긴 것이 된다.

환산은 `ρ[px] = PPM × H_head(0.25m)`, 즉 `PPM = 4ρ`.

| 기하 기준선 | 최소 PPM | ρ 임계 | WDR | 미달구역 | ΔWDR |
|---|---:|---:|---:|---:|---:|
| 임계 없음 (종전) | — | 0 | 0.6043 | 436 | +0.0953 |
| DORI detection | 25 | 6.25px | 0.6306 | 402 | +0.0690 |
| **DORI observation (기본값)** | 62.5 | 15.62px | **0.6682** | **334** | **+0.0314** |
| DORI recognition | 125 | 31.25px | 0.6582 | 334 | +0.0414 |

**기본값은 ΔWDR 이 가장 작은 observation** 으로 잡았다. 기존 방식에 가장 유리한
기준선을 골라야 "약한 상대와 싸웠다" 는 비판을 받지 않는다.

recognition 이 observation 보다 나쁜 것은 오류가 아니다. 임계를 올리면 만족
가능한 복셀이 급감해(통과 쌍 28.4% → 6.1%) 탐욕이 오히려 나빠진다.
**임계 방식 자체의 구조적 결함**이며 제안서 소재다.

### 2. 가시 쌍의 DORI 등급 분포 — 새로 나온 발견

가시 복셀-카메라 쌍 9,720개의 ρ 중앙값은 **11.35px (= 45 PPM)** 다.

| 등급 | 최소 PPM | 만족 쌍 | 비율 |
|---|---:|---:|---:|
| detection | 25 | 9,032 | 92.9% |
| observation | 62.5 | 2,762 | 28.4% |
| **recognition** | **125** | **597** | **6.1%** |
| identification | 250 | 109 | 1.1% |

안전모 착용 여부 판별에 필요한 Recognition 급을 만족하는 쌍은 **6.1%** 다.
나머지 93.9% 는 **"보이기는 하지만 판별할 픽셀이 없는"** 상태다.

### 3. θ·o 격자를 75 수준까지 넓히고 외삽을 막았다

종전에는 ρ 에만 measured_range 가드가 있었고 θ·o 는 외삽을 그대로 답했다.
한 축에만 외삽 금지를 적용한 비대칭은 나머지 방어논리까지 의심받게 한다.

격자 200 → **288조건** (ρ 8수준 × θ 6수준 × o 6수준). 새로 잰 값:

| θ (ρ=48, o=0) | 0 | 15 | 30 | 45 | 60 | **75** |
|---|---:|---:|---:|---:|---:|---:|
| `recall_nohat` | 0.9578 | 0.9592 | 0.9395 | 0.9046 | 0.5623 | **0.0146** |

| o (ρ=48, θ=0) | 0 | 15 | 30 | 45 | 60 | **75** |
|---|---:|---:|---:|---:|---:|---:|
| `recall_nohat` | 0.9578 | 0.6737 | 0.2389 | 0.0561 | 0.0036 | **0.0** |

`detect_model` 이 측정 상한 초과를 P=0 으로 본다. 실제로 잘려나간 양:

| 가드 | 해당 쌍 | 비율 (가시 9,720 중) |
|---|---:|---:|
| ρ < 4px | **0** | 0.00% |
| θ > 75° | 8 | 0.08% |
| o > 0.75 | 602 | 6.19% |
| 합계(중복 제거) | 610 | 6.28% |

**ρ 외삽 쌍이 0개다.** Phase 2 보강에서 ρ 를 8·6·4 로 넓힌 판단이 옳았고,
그 구멍은 완전히 닫혔다.

### 4. `g(θ)` 를 로지스틱으로 교체 — 단면 R² 0.7769 → 0.9973

θ=75 실측(0.0146)으로 절벽이 드러났다. 2차·지수는 이 모양을 맞히지 못한다.

| 후보 | 단면 R² |
|---|---:|
| quadratic | 0.8586 |
| exponential | 0.5125 |
| **logistic** | **0.9973** |

`g(θ) = (1+e^{-k·x0}) / (1 + e^{k(θ-x0)})`, **k=0.2420 · x0=61.36°**.
꼭짓점 문제도 사라진다(단조 감소가 형태로 보장됨). `x0=61.4°` 는 검출이
무너지는 부감각을 하나의 수로 준다 — 제안서에 바로 쓸 수 있는 값이다.

### 5. 개체 단위 가림률 검증 — 정의 차이는 없었다

실험은 화면 전체 가림률, 현장(§5.2)은 사람 막대 기준 개체 가림률이라 정의가
어긋난다고 봤다. `src/occ_box.py` 로 GT 박스별 가림 픽셀 비율을 재보니:

| 목표 o | 15 | 30 | 45 | 60 | 75 |
|---|---:|---:|---:|---:|---:|
| 인스턴스 평균 실측 | 15.11 | 30.32 | 45.05 | 60.10 | 75.01 |

**편차 최대 0.4%p.** 두 정의가 실측상 일치한다. 곡선은 정의가 맞는 쪽
(`occ_pct_box`)으로 피팅하도록 바꿨고, 이제 "검증했다"고 말할 수 있다.

### 6. 민감도 스윕 — 결론이 자유 파라미터에 얼마나 매달려 있는가

가림축이 세 축 중 압도적인데 그 축을 만드는 값 둘에 근거가 없었다.
`src/sensitivity.py` → `outputs/sensitivity.json`.

**(a) 스트라이프 주기.** 같은 가림률이라도 부재가 촘촘하면 급락한다.

| o=30% 에서 `recall_nohat` | div 12 | div 24 | div 48 |
|---|---:|---:|---:|
| | 0.4428 | 0.2389 | 0.0452 |

| 주기 | λ | WDR 기하 | WDR 확률 | ΔWDR |
|---|---:|---:|---:|---:|
| div 12 | 2.9688 | 0.7687 | 0.8022 | **+0.0335** |
| div 24 (기본) | 4.2007 | 0.6682 | 0.6996 | **+0.0314** |
| div 48 | 7.5427 | 0.4860 | 0.5109 | **+0.0249** |

스윕 범위는 임의가 아니다. 화면이 담는 장면 폭 `W = 2·d·tan(HFOV/2)` 이고
강관비계 지주 간격이 약 1.8m 이므로 `divisor ≈ d/0.9` — d=10m 에서 11,
20m 에서 22, 40m 에서 44 다. 12/24/48 이 그 범위를 덮는다.
**거리 의존을 모델에 넣지는 않았다.** 넣으면 분리형 곱셈 모델이 깨진다(§4.5).

**(b) 비계 시야 점유율.**

| SCAFFOLD_COVERAGE | 0.20 | 0.35 (기본) | 0.50 |
|---|---:|---:|---:|
| ΔWDR | +0.0356 | +0.0314 | +0.0266 |

**(c) 판정 임계.**

| 임계 | 미달 기하 | 미달 확률 | 감소 |
|---|---:|---:|---:|
| 0.4 | 238 | 132 | 106 |
| 0.5 (기본) | 334 | 168 | 166 |
| 0.6 | 458 | 394 | 64 |

**결론.** 절대 WDR 은 0.486 ~ 0.802 로 크게 흔들린다. 그러나
**ΔWDR 은 +0.0249 ~ +0.0356 으로 부호가 전부 유지된다.**
"파라미터를 바꾸면 뒤집히지 않느냐" 에 대한 답이 이 표다.
`sensitivity.json` 의 `sign_preserved: true`.

### 7. 최종 수치 — §2 목표 문장

| 기준 | WDR | 미달구역 |
|---|---:|---:|
| 기하 커버리지 (IEC 62676-4 DORI observation 준수) | **66.8%** | 334 |
| 검출확률 (제안) | **70.0%** | 168 |

ΔWDR **+3.1%p** · 미달구역 **-166개 (-49.7%)** · 공통 카메라 6대.

종전(+9.6%p)보다 작다. 기준선을 실무 수준으로 올렸기 때문이며 이쪽이 정직한 값이다.
**WDR 격차보다 미달구역 절반 감소가 더 강한 그림이다.**

미달 원인 분포 (원인은 세 축 중 가장 크게 깎은 것):

| 배치 | 원인 |
|---|---|
| 기하 (334) | 가림 249 · ρ 부족 32 · 화각 밖 21 · 완전 차폐 14 (+ 측정범위 밖) |
| 확률 (168) | 가림 127 · 완전 차폐 21 · 화각 밖 14 |

**미달의 대부분이 가림이고, ρ 부족은 확률 배치에서 사라진다.**
배치로 풀리는 것(거리)과 안 풀리는 것(가림)이 갈린다.

### 8. 목업 보강

- **`file://` 로 열어도 렌더링된다.** `report.py` 가 `data.js`(`window.SITE_DATA`)를
  함께 낸다. 프로토콜을 먼저 보고 `file:` 이면 fetch 를 아예 시도하지 않아
  실패 요청이 콘솔에 남지 않는다. 정본 경로는 여전히 `data.json` fetch 다.
  데이터를 HTML 에 하드코딩하지 않는다(§7 Phase 4 요건 유지). `mockup/serve.bat` 추가.
- **히트맵 기하/확률 토글.** 같은 8대를 같은 자로 잰 두 배치를 전환해 본다.
  `fail_zones` 를 배치마다 싣도록 `report.py` 를 고쳤다.
- **미달 원인 분포 한 줄 요약**을 표 위에 넣었다.
- **DORI 기준선 등급표**를 새 절로 추가했다.

Acceptance 재확인 — 로컬 서버로 띄워 브라우저에서 렌더 확인, **콘솔 메시지 0**,
토글 동작 확인(기하 334개 / 확률 168개로 목록이 바뀐다).

### 남은 것

- **VLM 대조 5조건(§4.3)이 미실행이다.** 모델 선택과 다운로드가 필요해 보류했다.
- 위험 가중치는 여전히 `hardcoded_provisional` 이다. LH 지수 원자료 미확보.
- 카메라 부각(pitch) 미검사는 그대로다.

---

## 부피 복셀화 이후 (2026-08-19) — 현재 정본

커밋 `78742e0` ~ `79c81bd` 의 기록이다. 앞 절이 쓰인 뒤에 들어간 변경이라 뒤늦게 채운다.

### 1. 복셀을 면에서 부피로 바꿨다 — 1,256 → 9,876 (`e12f283`)

종전 복셀은 작업면 높이대(z=0.5~2.0m) 한 층뿐이었다. 그러면 **CCTV 가 바닥만 본다고
가정한 것**이 된다. 실제로는 비계 위·갱폼 작업면·슬래브 단부처럼 공중에서 일어나는
작업이 사고의 큰 몫이다. `config.VOXEL_MODE = "volume"` 과 `VOXEL_Z_MAX_M = 14.0`
(비계 상단)을 넣고 층을 3개로 뒀다(`SLAB_LEVELS_M = [0.0, 4.3, 8.3]`).

복셀-카메라 쌍이 **237,024** 개가 됐고 그중 가시 쌍은 **67,641** 이다.

부피로 세면 아무도 갈 수 없는 허공이 분모에 섞이므로, 복셀마다 `occupiable` 판정을
달고 **지표는 사람이 있을 수 있는 자리만으로 낸다.** 시각화는 전부 그린다.

### 2. 세 번째 배치축 — 문헌의 가정 곡선 (`78742e0`)

종전에는 기하 vs 실측 둘만 비교했다. ADDENDUM-01 §5.4 를 반영해 **DORI recognition
(125 PPM)을 곡선처럼 쓴 가정 배치**를 하나 더 넣었다. 셋 다 실측 곡선의 자로 잰다.

| 배치 | WDR | 미달복셀 |
|---|---:|---:|
| ① 기하 커버리지 (DORI observation 준수) | **0.6151** | 1,186 |
| ② 문헌의 가정 곡선 (DORI recognition) | **0.6404** | 788 |
| ③ 실측 곡선 (제안) | **0.6607** | **284** |

ΔWDR 실측−기하 **+0.0456** · 실측−가정 +0.0203 · 가정−기하 +0.0253.
공통 카메라는 실측∩기하 6대 · 실측∩가정 6대.

**가정 곡선이 기하보다는 낫고 실측보다는 못하다.** "픽셀밀도 기준을 연속값으로 쓰기만
해도 절반쯤은 따라온다, 나머지 절반이 실측 곡선의 몫" 이라는 분해가 된다. 기여를
과장하지 않으면서 남기는 그림이다.

기하 기준선 등급별 (기본값은 종전과 같이 `observation`):

| 등급 | ρ 임계 | WDR | 미달 | ΔWDR |
|---|---:|---:|---:|---:|
| 임계 없음 | 0 | 0.6492 | 572 | +0.0115 |
| detection | 6.25px | 0.6368 | 659 | +0.0239 |
| **observation (기본)** | **15.62px** | **0.6151** | **1,186** | **+0.0456** |
| recognition | 31.25px | 0.5978 | 1,028 | +0.0629 |

> **자기 정정.** 08-15 절에는 observation 을 고른 이유를 *"ΔWDR 이 가장 작은, 즉 기존
> 방식에 가장 유리한 기준선"* 이라고 적었다. 부피 복셀화 뒤로는 그 말이 성립하지 않는다
> — 지금은 임계 없음이 최소(+0.0115)다. observation 을 유지하는 이유는 §5.4 의 다른
> 쪽 근거뿐이다: **실무 도구(JVSG·Axis)가 DORI 를 지키므로 임계 없는 기준선은 상대를
> 실무보다 못하게 모델링한 것이 된다.** 제안서에 "가장 유리한 기준선을 골랐다" 고 쓰지 말 것.

### 3. 가시 쌍의 DORI 등급 분포 — 부피 기준 재측정

ρ 중앙값 **11.62px** (최소 4.86 / 최대 339.41).

| 등급 | 최소 PPM | 만족 쌍 | 비율 |
|---|---:|---:|---:|
| detection | 25 | 62,641 | 92.6% |
| observation | 62.5 | 20,650 | 30.5% |
| **recognition** | **125** | **4,596** | **6.8%** |
| identification | 250 | 932 | 1.4% |

면 복셀 시절(6.1%)과 크게 다르지 않다. **안전모 착용 여부를 판별할 픽셀이 있는 쌍은
여전히 7% 미만이다.**

### 4. 외삽 가드가 잘라낸 양

| 가드 | 해당 쌍 | 비율 (가시 67,641 중) |
|---|---:|---:|
| ρ < 4px | **0** | 0.00% |
| θ > 75° | 8 | 0.01% |
| o > 0.75 | 3,241 | 4.79% |
| 합계(중복 제거) | 3,249 | 4.80% |

ρ 하한에 걸린 쌍이 부피 기준으로도 **0개**다.

### 5. 항목별 곡선 분리 (`9221cfb`)

ADDENDUM-01 §5.3 을 반영해 곡선을 항목마다 따로 뽑고 **종합은 최솟값**으로 낸다.
평균이 아니다 — 안전모 실패가 사람 검출 성공에 가려지면 논지가 사라진다.

| 항목 | 지표 | 전체 288점 R² |
|---|---|---:|
| `helmet_nohat` (주 지표) | `recall_nohat` | 0.8977 |
| `helmet_worn` | `recall_hat` | 0.8308 |

대표 R² 는 **가장 나쁜 항목**인 0.8308 로 쓴다(기준 0.80 통과). 부록이 요구한 4항목
(person/helmet/pose-fallen/pose-gesture) 중 자세 2종은 SHWD 에 라벨이 없어 미구현이다.
**항목을 늘리는 것은 곡선을 더 얹는 것뿐**이라 구조는 그대로 확장된다.

종합을 최솟값으로 바꿔도 수치는 거의 안 움직인다(WDR 0.6611 → 0.6607). 값어치는
숫자가 아니라 구조에 있다.

---

## 2026-08-19 (2) — 팀원 개선분 선별 반영

팀원이 보낸 `BIGB-fix-viewer-and-consistency.zip` 을 대조해 **일부만 가져왔다.**
zip 이 08-19 오전 스냅샷에서 갈라져 나와, 그 뒤 작업(항목별 곡선 · 선행기술 §9 ·
가이드라인 출처 정정 · 검출기 이력 컬럼)을 되돌리는 변경이 섞여 있었다.

### 가져온 것

| 대상 | 내용 |
|---|---|
| `mockup/viewer.js` | 회전 버그 4건 |
| `mockup/plan.js` (신규) | 현장 평면도 |
| `mockup/index.html` | 평면도 섹션 연결 |
| `src/safety_report.py` | `_shown_path()` — CLI 인자 크래시 |
| `config.py` | `DETECTOR_ARCH` `yolo26s` → `yolov8n` |

**뷰어 회전 — 실측 검증.** 3D 궤도 모드, 캔버스 1152×520, `showSolids` ·
`onlyOccupiable` 켠 상태로 yaw 를 15° 씩 한 바퀴 돌려 `_fit` 을 읽었다.

| | 배율 범위 | 출렁임 | 중심 이동 |
|---|---|---:|---:|
| 반영 전 | 8.344 ~ 9.839 | **17.9%** | **168.7px** |
| 반영 후 | 6.985 고정 | **0%** | **0px** |

매 프레임 투영 실루엣에 배율·중심을 다시 맞추던 것이 원인이었다. 궤도 모드에서는
yaw 전 구간의 최악 배율로 고정하고 중심을 캔버스 한가운데에 못박는다. 대가는 배율이
종전 최소값보다 16% 작아지는 것이고, 휠 줌으로 덮인다.

함께 들어온 것 — pitch 부호 반전(위로 끌면 내려다보기, OrbitControls 관례) · 음의
pitch 허용(상부 슬래브에 가린 위층을 아래에서 확인) · 회전 피벗을 모델 z중앙으로
(`_pivotZ`) · pitch 부호에 따른 밑면 선택.

**평면도.** 다른 화면은 전부 결과(히트맵·미달구역·WDR)인데 "그래서 어떤 현장을 잰
건가" 에 답하는 화면이 없었다. 치수선·축척·방위·위험구역 범례·카메라 화각이 들어간다.
화면에 **"가상 현장이다. 실제 LH 현장 도면이 아니다"** 를 박아뒀다 — 도면으로 오인되면
심사에서 문제가 되는 지점이다.

**`safety_report.py`.** `Path(plan_path).relative_to(config.ROOT)` 가 보고서에 찍을
표시용 경로 한 줄인데, 인자를 절대경로화하지 않아 **상대경로 인자를 주면 `ValueError`
로 보고서 전체가 안 나왔다.** 기본값 경로만 절대경로라 인자 없이 돌릴 때만 통과했다.
`_shown_path()` 로 끊었다.

**`DETECTOR_ARCH`.** `yolo26s` 로 적혀 있었으나 그 이름으로 학습한 적이 없다 —
`runs/detect/` 에 있는 것은 `shwd_yolo26n` 과 `shwd_yolov8n` 뿐이라
`DETECTOR_BEST` 가 없는 경로를 가리켰고 `run_grid.py` 가 `FileNotFoundError` 로 죽었다.
게다가 `grid_results.csv` 288행과 `curve_params.json` 이 전부 yolov8n 산출이라, 검출기
대조 로직이 288행을 통째로 버리고 없는 가중치로 재실행을 시도했다. **설정과 데이터를
일치시키려고** `yolov8n` 으로 되돌렸다.

> 팀원 주석은 되돌린 사유를 *"작업 PC 에 NVIDIA GPU 가 없다(Intel Arc · torch CPU)"*
> 로 적었는데 **그건 팀원 PC 얘기다.** 이 PC 는 RTX 3070 Ti · torch 2.7.1+cu118 ·
> CUDA 사용 가능이고 `shwd_yolo26n` 가중치도 이미 있다. 주석을 이 환경 기준으로
> 다시 썼다. 전환은 환경이 아니라 **격자 재측정(500장 × 288조건)을 할지 말지**의 문제다.

### 가져오지 않은 것 — 되돌림 4건

| 대상 | 되돌아갈 뻔한 것 |
|---|---|
| `ADDENDUM-01-prior-art.md` §9 (83줄) | 선행기술 리스크 R1·R2·R3 확인 결과. Sensors 2014 의 감쇠 파라미터가 원문에 **"trial and error"** 로 맞춘 값이라는 확인 등, 신규항목 #10·#11 이 유효하다는 판정 근거 전부 |
| `CLAUDE.md` §1 배경 | 「스마트 안전장비 활용 가이드라인」(국토교통부·국토안전관리원, 쪽·표번호 포함)과 KISA 인증을 구분한 정정을, 틀린 **"LH 가이드라인"** 서술로 되돌림 |
| `detect_model.MultiCurve` · `fit_curve` 다항목 | 위 5절의 항목별 곡선 구현 삭제 |
| `run_grid` `detector` 컬럼 + 이력 대조 | 검출기를 바꿔도 옛 행을 조용히 재사용하게 됨 |

### 곁들여 고친 것 — `file://` 폴백이 깨져 있었다

zip 과 무관하게 **우리 리포에 있던 버그다.** `mockup/index.html` 을 파일로 직접 열면
렌더링 대신 에러 박스가 떴다. §7 Phase 4 Acceptance("브라우저에서 파일 열었을 때
에러 없이 렌더링, 콘솔 경고 0")를 못 지키고 있었다.

원인은 **뷰어 배선 코드가 `else` 블록 안에 갇혀 있던 것**이다.

```js
if (location.protocol === 'file:') {
  try { show(window.SITE_DATA); }     // render() → mountViewer 호출
  catch (e) { fail(...); }            // ← 여기로 빠졌다
} else {
  let VIEWER = null;                  // 블록 스코프. 바깥에서 안 보인다
  function mountViewer(d, view) { ... }
  fetch('data.json')...
}
```

`file://` 경로가 `show()` 를 부르면 `render()` 안에서 `mountViewer` 를
`ReferenceError` 로 만나고, 그것을 폴백 실패로 오인해 에러 박스를 띄웠다.
커밋 `78742e0` 에 배선이 들어가면서 위치를 잘못 잡은 것이고, 팀원 zip 도 그대로
물려받았다. 배선을 최상위로 되돌렸다.

| 경로 | 반영 전 | 반영 후 |
|---|---|---|
| `http://` | 렌더 OK · 콘솔 0 | 렌더 OK · 콘솔 0 |
| `file://` | **에러 박스** · 콘솔 1 | **렌더 OK · 콘솔 0** |

`file://` 에서 카드(61.5 / 64.0 / 66.1 / 1186→284) · 평면도 · 3D 뷰어 · 범례 10항목이
모두 나오는 것을 확인했다. 배치 토글도 정상이다(기하 1,186 / 실측 284 로 목록이 바뀐다).

### 확인한 것

- **줄바꿈.** zip 은 전 파일이 LF 다. 리포는 CRLF 이므로 변환해 넣었다. 그대로 덮으면
  git 이 전 파일을 변경으로 잡아 진짜 diff 가 묻힌다.
- **곡선 계약 호환은 한 방향뿐이다.** 현재 코드는 zip 의 단일곡선 `curve_params.json`
  을 읽지만(`per_target` 없으면 `Curve` 로 물러남), **zip 코드는 현재 파일을 못 읽는다**
  (`KeyError: 'f_rho'`). 소스만 골라 받으면 파이프라인이 끊긴다.
- `outputs/safety_report.json` 은 이미 부피 복셀(9,876) 기준이라 재생성이 필요 없었다.
